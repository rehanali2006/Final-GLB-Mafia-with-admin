<% layout('/layouts/boilerplate') %>

<link rel="stylesheet" href="/css/chooseSubject.css">

<div class="subject-container">

    <div class="subject-header">

        <h1>Select Subject</h1>

        <p>
            Choose a subject to explore available
            <strong><%= type %></strong> resources
        </p>

    </div>

    <div class="subject-grid">

        <% if(currentYearSubjects.length === 0){ %>

            <p class="text-center w-100">
                No subjects have been added for this year yet.
            </p>

        <% } %>

        <% currentYearSubjects.forEach(subject => { %>

            <a
                href="/resource/<%= type %>/<%= encodeURIComponent(subject.name) %>"
                class="subject-card"
            >

                <div class="subject-icon">
                    <i class="bi bi-book-half"></i>
                </div>

                <h3><%= subject.name %></h3>

                <span class="view-btn">
                    View Resources →
                </span>

            </a>

        <% }) %>

    </div>

</div>